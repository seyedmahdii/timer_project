import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    buttons: {
        textAlign: "center",
        display: "flex",
        justifyContent: "center",
        flexWrap: "wrap",
        marginTop: "auto",
        marginBottom: 2,
    },
    handleButtons: {
        color: "white",
        fontSize: 50,
    },
}));
